import React from 'react';
import './RoundUI.css'; // Import the CSS file for styling

const RoundUI = () => {
    return (
        <div className="round-ui">
            <p>My Round UI</p>
        </div>
    );
}

export default RoundUI;